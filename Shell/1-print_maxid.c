#include <stdio.h>
#include <limits.h>
#include <unistd.h>
/**
  *main- prints the maximum value a PID can be
  *
  *Return: the maximum PID value
  */
int main(void)
{
	printf("Maximum PID value: %d\n", INT_MAX);
	return (0);
}
